# ポートフォリオ用サイト作成　作業手順書
Docker × Next.js × GitHub Pages 環境構築手順書
本書は、Windows環境において、ホスト環境を汚さずにDockerコンテナ内で「Next.js (App Router) + TypeScript + Tailwind CSS」の開発環境を構築し、GitHub Actionsを用いてGitHub Pagesへ自動デプロイ（CI/CD）を行うための全手順を記録したものである。

1. 構成・フォルダ構造
最終的に目指すディレクトリ配置は以下の通りである。全体の作業スペースとなるルート直下に、GitHub Actionsの設定ファイルと、Next.jsのソースコードを格納する frontend フォルダを分離して管理する。

Plaintext
C:\portfolio-web\
├── .github\
│   └── workflows\
│       └── deploy.yml      # GitHub Pagesへの自動デプロイ指示書
└── frontend\               # Next.jsプロジェクト（コンテナと同期）
    ├── src\                # ソースコード
    ├── compose.yml         # Docker Compose設定ファイル
    ├── Dockerfile          # コンテナビルド定義ファイル
    ├── .dockerignore       # コンテナへのコピー除外設定
    └── next.config.mjs     # Next.js設定ファイル（静的出力用）
2. 開発環境の構築（ローカル）
Step 1: プロジェクトフォルダの作成
パソコンの任意の場所にルートフォルダ portfolio-web を作成する。

作成したフォルダをVS Code等のエディタで開く。

ルート直下に、Next.js専用の空フォルダ frontend を作成する。

Step 2: Next.js プロジェクトの生成（Docker経由）
ホスト環境にNode.jsを直接インストールせず、一時的なDockerコンテナを立ち上げてNext.jsのファイルを安全に生成する。

PowerShellで C:\portfolio-web\frontend に移動する。

以下のコマンドを実行する。

PowerShell
docker run --rm -v ${PWD}:/app -w /app node:22-slim npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*"
ターミナルに表示される対話形式の質問には、すべて Yes と回答して進める。

Step 3: Docker設定ファイルの配置
frontend フォルダの直下に、以下の3つの設定ファイルを新規作成する。

📄 Dockerfile

Dockerfile
FROM node:22-slim
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
CMD ["npm", "run", "dev"]
📄 compose.yml

YAML
services:
  web:
    build: .
    volumes:
      - .:/app
      - /app/.next
      - /app/node_modules
    ports:
      - "3000:3000"
📄 .dockerignore

Plaintext
node_modules
.next
Step 4: Next.js の静的エクスポート設定
GitHub Pagesは静的ファイルしか配信できないため、Next.jsをSSG（静的サイト生成）モードに切り替える。
frontend/next.config.mjs を開き、中身を以下のように修正する。

JavaScript
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // 静的HTMLとして出力
  images: {
    unoptimized: true, // 画像最適化サーバーをオフにする
  },
};

export default nextConfig;
Step 5: 開発サーバーの起動確認
frontend フォルダ内で以下のコマンドを実行し、コンテナを立ち上げる。

PowerShell
docker compose up --build
起動完了後、ブラウザで http://localhost:3000 にアクセスし、Next.jsの初期画面が表示されればローカル環境構築は完了。

3. 自動デプロイ環境の構築（CI/CD）
Step 1: GitHub Actions ワークフローの作成
リポジトリの直下（ルート階層）に .github/workflows フォルダを作成し、その中に deploy.yml を作成する。パスが frontend の中に入らないよう注意すること。

📄 .github/workflows/deploy.yml

YAML
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: './frontend/package-lock.json'

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Install dependencies
        run: npm ci

      - name: Build with Next.js
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./frontend/out

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
Step 2: GitHubへの初プッシュ
GitHub上で新しいパブリックリポジトリ（例: my-portfolio）を作成する。

PowerShellでルートディレクトリ（C:\portfolio-web）に移動し、以下のGitコマンドを実行してコードを送信する。

PowerShell
   git init
   git add .
   git commit -m "Initial commit with Docker and Next.js"
   git branch -M main
   git remote add origin https://github.com/あなたのユーザー名/リポジトリ名.git
   git push -u origin main
Step 3: GitHub Pagesの配信元設定
GitHubのリポジトリ画面を開き、「Settings」 タブ ➔ 左メニューの 「Pages」 をクリックする。

Build and deployment にある Source の項目を、「Deploy from a branch」から 「GitHub Actions」 に変更する。

4. 運用の流れ（今後の開発手順）
環境構築完了後の日常的な開発フローは以下の通りとなる。

ローカルで開発を行う場合
C:\portfolio-web\frontend で docker compose up を実行し、コードを修正する（ホットリロード対応）。

本番環境へ反映する場合
Gitでコミットし、GitHubへプッシュする。

PowerShell
   git add .
   git commit -m "任意のコミットメッセージ"
   git push origin main
反映の確認
GitHubの「Actions」タブでビルドが成功（✅）したことを確認すると、数分後に自動的にGitHub PagesのURL（ https://<ユーザー名>.github.io/<リポジトリ名>/ ）へ変更が反映される。